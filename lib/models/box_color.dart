import 'dart:ui';

class BoxColor {
  Color color;
  Color colorText;

  Color dmColor;
  Color dmColorText;

  String textValue;

  BoxColor(this.color, this.colorText, this.dmColor, this.dmColorText, this.textValue);
}